import './signaturesPage.scss';

const SignaturesPage = () => {
    return (
        <div className='signatures-page'>
            <h1>{`Signatures are currently unavailable :(`}</h1>
        </div>
    );
};

export default SignaturesPage;
